// Test file to verify array push/pop and HashMap entries methods work correctly
fun main() {
    // Test array push/pop
    var arr = [1, 2, 3]
    println("Original array length: " + arr.length().toString())
    
    arr.push(4)
    println("After push(4) length: " + arr.length().toString())
    
    var popped = arr.pop()
    println("Popped element: " + popped)
    println("After pop() length: " + arr.length().toString())
    
    // Test HashMap entries
    var map = {"a": 1, "b": 2, "c": 3}
    println("HashMap size: " + map.size().toString())
    
    var entries = map.entries()
    println("Entries count: " + entries.size().toString())
    
    var keys = map.keys()
    println("Keys count: " + keys.size().toString())
    
    var values = map.values()
    println("Values count: " + values.size().toString())
    
    var size = map.size()
    println("Size: " + size)
    
    println("All tests completed successfully!")
}