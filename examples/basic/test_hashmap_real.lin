fun main() {
    // Test HashMap literal functionality
    var map = { "key1": 100, "key2": 200, "key3": 300 }
    println("HashMap created with 3 entries")
    
    // Test array functionality
    var arr = [10, 20, 30]
    println("Array created with 3 elements")
    
    var first = arr[0]
    var second = arr[1] 
    var third = arr[2]
    
    println(first)
    println(second) 
    println(third)
    
    // Test string operations
    var greeting = "Hello"
    println(greeting.length)
    
    println("HashMap and Array functionality working!")
}